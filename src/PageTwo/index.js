import React from 'react'
import Button from 'react-bootstrap/Button'

export default function PageTwo(props) {
    return (
        <div>
            <p>New goal Model (peewee) goes here.</p>
            <Button>Back to Jobs</Button>
        </div>
    )
}
